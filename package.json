{
  "scripts": {
    "dev": "vite",
    "build": "vite build",
    "postbuild": "node copy-assets.js",
    "serve": "vite preview",
    "predeploy": "npm run build",
    "deploy": "gh-pages -d dist"
  },
  "type": "module",
  "dependencies": {
    "three": "^0.169.0",
    "vite": "^5.4.8"
  },
  "devDependencies": {
    "gh-pages": "^6.2.0"
  }
}
